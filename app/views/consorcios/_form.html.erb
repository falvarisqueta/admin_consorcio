<%= form_with(model: consorcio, local: true) do |form| %>
  <div>
    <h4>Consorcio</h4>
  </div>

  <% if @consorcio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(consorcio.errors.count, "error") %> prohibited this consorcio from being saved:</h2>

      <ul>
        <% @consorcio.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <div class="form-group">
      <%= form.label :nombre %>
      <%= form.text_field :nombre, class: 'form-control'%>
    </div>
    <div class="form-group">
      <%= form.label :direccion %>
      <%= form.text_field :direccion, class: 'form-control'%>
    </div>
  </div>

  <div class="actions">
    <br/> <%= form.submit %>
  </div>
<% end %>
